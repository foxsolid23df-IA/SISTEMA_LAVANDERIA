import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.foxsolid.lavanderia',
  appName: 'Sistema Lavanderia',
  webDir: '../dist'
};

export default config;
